import fixture from './data/fixture.json'

const state = {
	fixture: fixture.fixture
};

const getters = {};

const actions = {};

const mutations = {};

export default {
	namespaced: true,
	state,
	getters,
	actions,
	mutations
}